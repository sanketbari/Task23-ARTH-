#run these commands in bash shell to add them as environment variables. The ec2.py program need these credentials to connect to AWS profile !!

export AWS_ACCESS_KEY_ID=AKIA***03N
export AWS_SECRET_ACCESS_KEY=Fu8d****Aizx
export AWS_REGION=<region name>
